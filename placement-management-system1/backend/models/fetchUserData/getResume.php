<?php

// session_start();
// require "../../config/database.php";

// $db = new Database();
// $conn = $db->connect();

// $stmt = $conn->prepare("SELECT resume FROM users_persontal_details WHERE id=?");
// $stmt->execute([$_SESSION["user_id"]]);

// $row = $stmt->fetch(PDO::FETCH_ASSOC);

// header("Content-Type: application/pdf");
// echo $row["resume"];
?>